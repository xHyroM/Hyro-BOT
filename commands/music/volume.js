const Discord = require("discord.js")
const bot = new Discord.Client()

module.exports = {
    name: "volume",
    aliases: ["volume"],
    category: "music", 
    usage: "",
    run: (client, message, args) => {}}