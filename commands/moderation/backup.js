const Discord = require("discord.js");
const fs = require('fs');

module.exports = {
    name: "backup",
    aliases: ["backup", "serverbackup"],
    category: "moderation",
    run: async (client, message, args) => {
      
    const embed = new Discord.MessageEmbed()
      .setTitle("BACKUP SYSTEM")
      .setDescription("Please invite: [here]()")
 
}}